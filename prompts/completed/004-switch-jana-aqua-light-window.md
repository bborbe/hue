---
status: completed
summary: Gave Jana Aqua Light its own fixed 12:30-22:30 Europe/Berlin window decoupled from the shared aquarium schedule, extended checks-creator tests to cover minute-precision boundaries under both summer modes, and added a CHANGELOG feat entry
execution_id: hue-exec-004-switch-jana-aqua-light-window
dark-factory-version: dev
created: "2026-08-29T09:57:13Z"
queued: "2026-08-29T10:03:33Z"
started: "2026-08-29T10:05:05Z"
completed: "2026-08-29T10:06:57Z"
---

<summary>
- "Jana Aqua Light" gets its own dedicated light window: on at 12:30, off at 22:30 Europe/Berlin (10h on), instead of sharing the generic aquarium window
- The change is constants-only — two fixed times (12:30 and 22:30) replace the shared schedule variables in just the one Jana switch; no new types, no signature changes, no new config
- The window is fixed regardless of `SUMMER_MODE` — no summer/winter branching is added for Jana
- Every other aquarium schedule stays exactly as today: Aquarium Licht, Aquarium Rack, Aquarium CO2, Garnelen Licht 1/2 keep the shared window; Artemia Licht stays 8-23; Aquarium Skimmer and Jana Aqua Skimmer stay 5min-on/25min-off alternate switches
- A unit test extension in the existing checks-creator test covers the new 12:30/22:30 minute-precision boundaries, run under both summer modes to prove the window is fixed
- A decoupling assertion proves Jana no longer follows the shared window (Jana off while Aquarium Licht on at 22:45 in summer mode, and vice versa at 21:00 in winter mode)
- CHANGELOG gains a `## Unreleased` section with a `feat:` entry describing the dedicated window
- `make precommit` (ensure + format + generate + test + check + addlicense) is the acceptance gate
</summary>

<objective>
Give "Jana Aqua Light" its own fixed light window — on 12:30, off 22:30 Europe/Berlin — so Jana's schedule is decoupled from the shared aquarium window and stays fixed regardless of `SUMMER_MODE`. This is a constants-only change: swap the shared-variable references in the Jana `NewBetweenTimeSwitch` block for two fixed `pkg.TimeOfDay` literals, extend the existing checks-creator test to cover the new window, and add a CHANGELOG entry.
</objective>

<context>
The owning task (Impact + Success Criteria): `/Users/bborbe/Documents/Obsidian/Personal/24 Tasks/Switch Jana aquarium light schedule to 12-30-22-30.md` (host-side orientation — content mirrored in Step 9) — success criteria require the Jana `BetweenTimeSwitch` to use its own 12:30 → 22:30 Europe/Berlin window independent of `aquariumLightOnHour` / `aquariumLightOffhour`, all other aquarium schedules unchanged, no summer-mode behavior for Jana, and a unit test covering the new window.

Read first:

- `./CLAUDE.md` — project conventions: `make precommit` is the gate, Ginkgo v2 / Gomega, external test packages (`package check_test`), counterfeiter mocks in `mocks/` (regenerated by `make generate`), `github.com/bborbe/errors` with `errors.Wrap(ctx, err, "...")`.
- `./.dark-factory.yaml` — `worktree: false`, `pr: false`, `autoRelease: false`, `validationPrompt: docs/dod.md`. Do NOT commit — dark-factory handles git.
- `./docs/dod.md` — DoD: Code Quality / Testing / Documentation; CHANGELOG must have an entry under `## Unreleased`.
- `./pkg/check/checks-creator.go` — the change site. `NewCheckCreator(provider pkg.BridgesProvider, summerMode bool, currentDateTimeGetter libtime.CurrentDateTimeGetter) CheckCreator`; `CreateChecks(ctx)` loads `loc` via `time.LoadLocation(c.location)` at the top, then returns a flat `Checks` slice of 9 switches. The "Jana Aqua Light" `NewBetweenTimeSwitch` block is the 8th switch in the return slice — the one using `NewLightIsOn(bridge, "Jana Aqua Light")` / `NewLightIsOff(bridge, "Jana Aqua Light")` with `pkg.TimeOfDay{Hour: aquariumLightOnHour, ...}` / `pkg.TimeOfDay{Hour: aquariumLightOffhour, ...}`. It sits between the "Aquarium Skimmer" `NewAlternateSwitch` (7th) and the "Jana Aqua Skimmer" `NewAlternateSwitch` (9th). The shared `aquariumLightOnHour` / `aquariumLightOffhour` variables (10-20 daytime, 20-23 summer mode) feed the Aquarium Licht, Aquarium Rack, Aquarium CO2, Garnelen Licht 1, Garnelen Licht 2, and Jana Aqua Light blocks.
- `./pkg/time-of-day.go` — `pkg.TimeOfDay` already has a `Minute int` field (struct: `Hour`, `Minute`, `Second`, `Location *time.Location`); `String()` renders `%02d:%02d:%02d` (e.g. `12:30:00`), which is what the `now is between ... and ...` log line prints. No type change needed.
- `./pkg/check/between-time-switch.go` — `NewBetweenTimeSwitch(now time.Time, from, until pkg.TimeOfDay, main, fallback Check) Check`. The window is evaluated at construction time against the injected `now` (via `NewSwitch` calling the predicate immediately); the returned check's `Name()` is either the main or the fallback's name (`"Light 'Jana Aqua Light' is on"` / `"... is off"`). The `from`/`until` values are NOT exposed on the returned `Check` — tests must assert the window via `Name()` at a fixed clock instant, exactly as the existing schedule test does.
- `./pkg/check/checks-creator_test.go` — the file to EXTEND (it already exists). Contains the in-tree patterns to reuse verbatim: the `fixedClock` struct (satisfies `libtime.CurrentDateTimeGetter` via `func (f fixedClock) Now() libtime.DateTime { return libtime.DateTime(f.t) }`), the `checkNamed(checks check.Checks, substr string) check.Check` helper (returns the check whose `Name()` contains `substr`; `"Jana Aqua Light"` matches only the Jana Light check, not "Jana Aqua Skimmer"), the `DescribeTable` shape, the `BeforeEach` that builds a `pkg.BridgesProviderFunc` returning `[]*huego.Bridge{nil}`, and the instant-precision assertion style (`stdtime.Date(2026, 6, 15, 21, 0, 0, 0, berlin)` + `Expect(c.Name()).To(ContainSubstring(expected))`).
- Pattern exemplars from discovery searches:
  - Error wrapping (`errors.Wrapf` / `fmt.Errorf`): `./pkg/check/checks-cron.go` (`errors.Wrapf(ctx, err, "create checks failed")`).
  - Counterfeiter directive: `./pkg/check/check.go` (`//counterfeiter:generate -o ../../mocks/check.go --fake-name Check . Check`).
  - Ginkgo tests: `./pkg/check/checks-creator_test.go` and `./pkg/check/alternate-switch_test.go` (the repo imports ginkgo/gomega dot-style, so a `ginkgo.` literal search finds nothing; these two files are the exemplars).

Coding plugin guides (container path — read if a convention is unclear):
- `/home/node/.claude/plugins/marketplaces/coding/docs/go-time-injection.md` — the fixed-clock injection pattern this test relies on.
- `/home/node/.claude/plugins/marketplaces/coding/docs/go-testing-guide.md` — Ginkgo v2 / Gomega conventions.
- `/home/node/.claude/plugins/marketplaces/coding/docs/changelog-guide.md` — `## Unreleased` placement (directly above the highest `## vX.Y.Z`, never inside the frozen SemVer preamble) and required conventional prefixes (`feat:` → minor bump).

This is a translation task with in-tree exemplars — Level 3 (medium) detail: contracts + pattern references + test scenarios, no inlined test bodies.
</context>

<requirements>

### Step 1 — Give "Jana Aqua Light" its own fixed window (`pkg/check/checks-creator.go`)

1. In `CreateChecks`, locate the "Jana Aqua Light" `NewBetweenTimeSwitch` block — the one whose `TimeOfDay` literals use `aquariumLightOnHour` / `aquariumLightOffhour` and whose lights are `NewLightIsOn(bridge, "Jana Aqua Light")` / `NewLightIsOff(bridge, "Jana Aqua Light")`. It is the 8th switch in the returned `Checks` slice, between the "Aquarium Skimmer" `NewAlternateSwitch` and the "Jana Aqua Skimmer" `NewAlternateSwitch`.

2. Replace ONLY the two `pkg.TimeOfDay` literals inside that block with fixed values. The `now` argument, the light constructors, and the block's position in the slice stay unchanged. The block becomes:
   ```go
   NewBetweenTimeSwitch(
       now,
       pkg.TimeOfDay{
           Hour:     12,
           Minute:   30,
           Location: loc,
       },
       pkg.TimeOfDay{
           Hour:     22,
           Minute:   30,
           Location: loc,
       },
       NewLightIsOn(bridge, "Jana Aqua Light"),
       NewLightIsOff(bridge, "Jana Aqua Light"),
   ),
   ```
   `loc` is the `*time.Location` from `time.LoadLocation(c.location)` already in scope at the top of `CreateChecks` — do not load a new location. `pkg.TimeOfDay.Minute` already exists; there is no type change and no import change.

3. Do NOT introduce variables for these hours (`12`, `30`, `22`) and do NOT branch on `c.summerMode` anywhere in or around the Jana block — the window is fixed and hardcoded. Do NOT touch the shared `var aquariumLightOnHour int` / `aquariumLightOffhour` block (the summerMode if/else stays exactly as it is), and do NOT touch any other switch in the slice:
   - Artemia Licht (8/23), Aquarium Licht, Aquarium Rack, Aquarium CO2, Garnelen Licht 1, Garnelen Licht 2 — keep the shared `aquariumLightOnHour` / `aquariumLightOffhour` references unchanged.
   - "Aquarium Skimmer" and "Jana Aqua Skimmer" `NewAlternateSwitch` blocks (5 min on / 25 min off) — unchanged.
   - The `CheckCreator` interface, `NewCheckCreator` signature, and `checkCreator` struct — unchanged.

### Step 2 — Extend the checks-creator test (`pkg/check/checks-creator_test.go`)

4. EXTEND the existing file `./pkg/check/checks-creator_test.go` — do not create a new file, do not delete or rewrite the two existing `Describe` blocks ("CheckCreator" aquarium-window count table and "CheckCreator schedule" 21:00 table). Those existing tests must stay green unchanged: the 9-entry count is stable (Jana is still one between-time-switch) and the shared-window behavior at 21:00 is untouched.

5. Add a new `Describe` block (e.g. `Describe("CheckCreator Jana Aqua Light window", ...)`) following the exact shape of the existing "CheckCreator schedule" `DescribeTable`: `BeforeEach` sets up `ctx`, `berlin` (`stdtime.LoadLocation("Europe/Berlin")`), and the `pkg.BridgesProviderFunc` returning `[]*huego.Bridge{nil}`; each table entry builds `check.NewCheckCreator(provider, summerMode, clock)` with a `fixedClock` at the given Berlin instant, calls `creator.CreateChecks(ctx)`, asserts `Expect(err).NotTo(HaveOccurred())`, then `c := checkNamed(checks, "Jana Aqua Light")`, `Expect(c).NotTo(BeNil())`, and `Expect(c.Name()).To(ContainSubstring(expected))`.

6. Table the minute-precision window boundaries at instant `stdtime.Date(2026, 6, 15, <h>, <m>, <s>, 0, berlin)`. Run EVERY entry twice — once with `summerMode = false` and once with `summerMode = true`, with the SAME expected outcome — to prove the window is fixed regardless of `SUMMER_MODE`:
   - 12:30:00 → `"is on"` (window start is inclusive)
   - 12:29:59 → `"is off"` (one second before start — proves `Minute` is honored, not just `Hour`)
   - 12:45:00 → `"is on"` (mid-window sanity)
   - 22:29:59 → `"is on"` (one second before end)
   - 22:30:01 → `"is off"` (one second after end — the 22:30 off boundary)

7. Add one decoupling `It` (or an equivalent table with two entries) asserting the Jana window is independent of the shared window:
   - At 22:45 Berlin with `summerMode = true`: `checkNamed(checks, "Jana Aqua Light")` must be `"is off"` (Jana already off at 22:45) while `checkNamed(checks, "Aquarium Licht")` must be `"is on"` (shared summer window 20-23 still active). This proves Jana no longer rides the shared summer window.
   - At 21:00 Berlin with `summerMode = false`: `checkNamed(checks, "Jana Aqua Light")` must be `"is on"` (12:30-22:30) while `checkNamed(checks, "Aquarium Licht")` must be `"is off"` (shared winter window 10-20). This proves Jana runs later than the shared winter window.

### Step 3 — CHANGELOG entry (`CHANGELOG.md`)

8. In `./CHANGELOG.md`, add a `## Unreleased` section immediately after the frozen SemVer preamble (the `# Changelog` title, "All notable changes..." line, SemVer link, and MAJOR/MINOR/PATCH bullets) and directly above the current highest version section (`## v0.2.8`). Do NOT move, delete, or insert anything inside the preamble. There is currently no `## Unreleased` section — create it. The section and bullet:
   ```markdown
   ## Unreleased

   - feat: Give "Jana Aqua Light" its own dedicated light window (on 12:30, off 22:30 Europe/Berlin) instead of sharing the aquarium window, fixed regardless of `SUMMER_MODE`. All other aquarium schedules (Aquarium Licht, Aquarium Rack, Aquarium CO2, Garnelen Licht 1/2, Artemia Licht, Aquarium Skimmer, Jana Aqua Skimmer) unchanged.
   ```

### Step 4 — Self-check against the owning task's success criteria

9. Before finishing, walk each success criterion from the owning task against the change:
   - "Jana Aqua Light" `BetweenTimeSwitch` uses its own 12:30 → 22:30 Europe/Berlin window, independent of `aquariumLightOnHour` / `aquariumLightOffhour` — verified by step 1 and the decoupling assertions.
   - Other aquarium schedules unchanged — verified by leaving every other switch untouched.
   - Unit test covers the new window asserting 12:30 on / 22:30 off — verified by the new table.
   - No summer-mode behavior applied to Jana — verified by the both-modes table and the absence of any `c.summerMode` reference in the Jana block.

### Step 5 — Acceptance gate

10. Run `make precommit` from the repo root. It must pass (ensure + format + generate + test + check + addlicense). If it fails for a reason unrelated to this change, STOP and report rather than working around it.
</requirements>

<constraints>
- Do NOT commit — dark-factory handles git (`.dark-factory.yaml`: `worktree: false`, `pr: false`, `autoRelease: false`; do NOT push, do NOT open a PR).
- Do NOT add summer-mode behavior for Jana — the hardcoded 12:30/22:30 window is the allowed shape; no `c.summerMode` reference may appear in or around the Jana block.
- Do NOT touch the shared `aquariumLightOnHour` / `aquariumLightOffhour` block, its summerMode if/else, or any other switch in `CreateChecks` (Artemia 8-23; Aquarium Licht / Rack / CO2 / Garnelen 1/2 shared window; Aquarium Skimmer and Jana Aqua Skimmer 5min/25min alternate switches).
- Do NOT change the `CheckCreator` interface, `NewCheckCreator` signature, `checkCreator` struct, or `pkg.TimeOfDay` — this is constants-only.
- Do NOT modify or delete the two existing `Describe` blocks in `checks-creator_test.go` — extend the file only.
- Do NOT edit the `LICENSE` file or the license header (lines 1-3) of any Go file.
- Do NOT bump `go.mod` / `go.sum`, do NOT run `go mod tidy` or `go mod vendor` — no dependency change.
- Do NOT reformat / reorder / modernize code outside the literal changes described above.
- CHANGELOG: `## Unreleased` goes directly above `## v0.2.8`, never inside or above the frozen preamble; flat list, no `### Added` categories; bullet must keep the `feat:` conventional prefix.
- `make precommit` MUST pass at the end (DoD per `docs/dod.md`).
- Do NOT number the prompt filename; do NOT modify any frontmatter field beyond `status` and `created`.
</constraints>

<verification>
1. `make precommit` from the repo root — must pass.
2. In `pkg/check/checks-creator.go`, the "Jana Aqua Light" `NewBetweenTimeSwitch` block must contain `Hour:     12` with `Minute:   30` and `Hour:     22` with `Minute:   30` and `Location: loc`, and must NOT reference `aquariumLightOnHour` / `aquariumLightOffhour` (grep the block's literals). The `if c.summerMode { ... } else { ... }` shared-window block must be byte-identical to before (only the Jana block changed).
3. In `pkg/check/checks-creator.go`, the other five shared-window switches (Artemia excluded) must still reference `aquariumLightOnHour` / `aquariumLightOffhour` — grep must still find those references in the Aquarium Licht, Aquarium Rack, Garnelen Licht 1, and Garnelen Licht 2 blocks (Aquarium CO2 uses the derived co2OnHour/co2OffHour, not the raw variables), and no reference anywhere near the Jana block.
4. `grep -c 'c.summerMode' pkg/check/checks-creator.go` — must show exactly 1 match (the shared-window if/else only); the Jana block must not add a second one.
5. In `pkg/check/checks-creator_test.go`, the new table must contain instants with minute precision `30` at both the on (12) and off (22) boundaries (e.g. `12, 30` and `22, 30`), and each boundary must be asserted under BOTH `summerMode` values with the same expected outcome (a test asserting the Jana window under only one mode fails the fixed-window requirement).
6. `grep -n '## Unreleased' CHANGELOG.md` — must show a section directly above `## v0.2.8`, with the `feat:` bullet from step 8 and no change to the frozen preamble.
7. If any file other than `pkg/check/checks-creator.go`, `pkg/check/checks-creator_test.go`, and `CHANGELOG.md` was modified, STOP and report — the scope was exceeded.
</verification>
