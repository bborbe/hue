# CLAUDE.md

Philips Hue desired-state controller — reconciles light state on a schedule using time-of-day / sunrise-sunset triggers, with multi-bridge discovery and TeamVault-backed credentials at deploy time.

## Dark Factory Workflow

**Never code directly.** All code changes go through the dark-factory pipeline.

### Complete Flow

**Spec-based (multi-prompt features):**

1. Create spec → `/dark-factory:create-spec`
2. Audit spec → `/dark-factory:audit-spec`
3. User confirms → `dark-factory spec approve <name>`
4. dark-factory auto-generates prompts from spec
5. Audit prompts → `/dark-factory:audit-prompt`
6. User confirms → `dark-factory prompt approve <name>`
7. Start daemon → `dark-factory daemon` (use Bash `run_in_background: true`)
8. dark-factory executes prompts automatically

**Standalone prompts (simple changes):**

1. Create prompt → `/dark-factory:create-prompt`
2. Audit prompt → `/dark-factory:audit-prompt`
3. User confirms → `dark-factory prompt approve <name>`
4. Start daemon → `dark-factory daemon` (use Bash `run_in_background: true`)
5. dark-factory executes prompt automatically

### Key rules

- Prompts go to **`prompts/`** (inbox) — never to `prompts/in-progress/` or `prompts/completed/`
- Specs go to **`specs/`** (inbox) — never to `specs/in-progress/` or `specs/completed/`
- Never number filenames — dark-factory assigns numbers on approve
- Never manually edit frontmatter status — use CLI commands
- Always audit before approving (`/dark-factory:audit-prompt`, `/dark-factory:audit-spec`)
- **BLOCKING: Never run `dark-factory prompt approve`, `dark-factory spec approve`, or `dark-factory daemon` without explicit user confirmation.**
- **Before starting daemon** — run `dark-factory status` first to check if one is already running
- **Start daemon in background** — Bash `run_in_background: true` (not foreground, not `&`)

## Development Standards

Follows the [coding-guidelines](https://github.com/bborbe/coding-guidelines).

- `make precommit` — ensure + format + generate + test + check + addlicense (MANDATORY before commits)
- `make test` — tests only (`-race` opt-in via `ENABLE_RACE=true`)
- `make check` — vet + errcheck + vulncheck + osv-scanner + trivy (lint not yet in `check`, see TODO in Makefile)
- `make buca` — build + upload + clean + apply (Docker + k8s deploy)
- Tool versions pinned via `tools.env` — keep in sync with sibling repos (run, kafka-topic-reader)

### Test conventions

- Ginkgo v2 / Gomega
- Counterfeiter mocks in `mocks/` (regenerated by `make generate`)
- External test packages (`pkg_test`)

## Architecture

- `main.go` — `application` struct + HTTP server (`/metrics`), wires factory, runs checks-cron
- `cmd/create-token/` — Hue bridge user creation CLI
- `cmd/{list-lights,turnon-light,turnoff-light}/` — operational CLIs
- `pkg/` — domain types: `Light`, `Token`, `time-of-day`, bridges-provider variants (cache/fallback/func)
- `pkg/factory/` — dependency wiring; pure composition, no business logic
- `pkg/check/` — desired-state checks (`alternate-switch`, `between-time-switch`, `between-minute-switch`, `light-is-on`, `light-is-off`), driven by `checks-cron` + `checks-runner`
- `pkg/trigger/` — schedule triggers (`right-away`, `time-of-day`)
- `k8s/` — deploy manifests; `k8s.env` holds `NAMESPACE`, `CLUSTER_CONTEXT=quant`, TeamVault key IDs

## Key Design Decisions

- **Multi-bridge with fallback** — `BridgesProvider` is composed (`func` → `cache` → `fallback`); never collapse into a single struct
- **Factory functions are pure composition** — no conditionals, no I/O, no `context.Background()`
- **Use `github.com/bborbe/time`** instead of stdlib `time` — inject `CurrentDateTime` in constructors
- **Use `github.com/bborbe/errors`** with `errors.Wrap(ctx, err, "...")` — never bare `return err`
- **Use `github.com/bborbe/collection.Ptr()`** instead of custom pointer helpers
- **Counterfeiter mock-gen via `//go:generate`** — keep `COUNTERFEITER_VERSION` in `tools.env` aligned with the directive
- **TeamVault at deploy, not runtime** — no secrets in repo; k8s manifests use `{{ "KEY" | env | teamvault* | base64 }}` templating
- **Deploy target is `quant` cluster** — `kubectlquant -n hue`
